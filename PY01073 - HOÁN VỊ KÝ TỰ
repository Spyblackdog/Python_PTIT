from itertools import permutations

s = list(input()) # Chuyển input đầu vào thành list
perm  = permutations(s) # Tạo một list bằng hàm permutations hàm sinh ra hoán vị
for x in perm:
    print("".join(x))   # In ra với "".join(x) nối các phần tử trong x với ""
