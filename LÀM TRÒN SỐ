t = int(input())
while t > 0:
    s = [int(x) for x in input()]
    for x in range(len(s)-1,0,-1):
        if int(s[x]) >= 5:
            s[x-1] += 1
        s[x] = 0
        if s[0] == 10:
            s[0] = 0
            s = [1] + s
    for x in range(len(s)):
        print(s[x],end = '')
    print()
    t -= 1
